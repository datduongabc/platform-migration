__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.venv/
venv/
tests/
.git/
.gitignore
Dockerfile
.dockerignore
.env
.env.example

# Runtime data mounted as a volume at deploy time (see docker-compose.yml) —
# must not be baked into the image or bloat the build context.
uploads/
